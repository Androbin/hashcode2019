import java.util.*;

public class Parser {
	private Parser() {}

	public static List<List<String>> parse(String[] lines) {
		// Sanity check
		assert Integer.parseInt(lines[0]) == lines.length - 1;

		List<List<String>> result = new LinkedList<>();
		for (int i = 1; i < lines.length; ++i) {
			
		}
	}
}
